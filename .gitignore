# ============================================================
# PobTools — .gitignore
# ============================================================

# --- Build outputs -----------------------------------------
*.obj
*.lib
*.exp
*.pdb
*.ilk
*.log
*.tlog/
x64/
Debug/
Release/

# pob-zh-engine build tree (CMake) and deployment bundle
pob-zh-engine/build/
pob-zh-engine/vcpkg/
pob-zh-engine/tests/translate_test_out.txt
pob-zh-engine/tests/display_colour_test_out.txt

# dist/ is the deployment bundle — a build output, so it stays ignored, with one
# exception: the translation dictionaries. Those ship in the public release zip
# already, and keeping them here is what lets an outside translator fork the
# repo, edit a dictionary and open a PR.
#
# ⚠ This MUST be written as `dist/*` plus per-level re-includes, never as
# `dist/` plus `!dist/Data/...`. Git does not descend into a directory that is
# excluded as a whole, so a `!` line underneath one would match nothing and the
# dictionaries would stay invisible — silently, with no error to explain it.
#
# Each re-included directory needs its parent re-included first, hence the
# Data/ line before the Data/* line.
pob-zh-engine/dist/*
!pob-zh-engine/dist/Data/
pob-zh-engine/dist/Data/*
!pob-zh-engine/dist/Data/poe1/
!pob-zh-engine/dist/Data/poe2/
!pob-zh-engine/dist/Data/launcher/

# Korean release runtime assets are source inputs for the public package.
!pob-zh-engine/dist/Fonts/
pob-zh-engine/dist/Fonts/*
!pob-zh-engine/dist/Fonts/NotoSansKR-Variable.ttf
!pob-zh-engine/dist/Fonts/OFL-NotoSansKR.txt
!pob-zh-engine/dist/pob-zh.ini

# 這裡只再收錄「dist 是唯一寫入者」的那幾組字典。三個曾經也列在這裡的檔
# (filter_items_zh.json / item_meta.json / item_classes_zh.json)已經拿掉了:
# 它們由 CMakeLists.txt 的 install(FILES host/data/...) 產生,dist 那份是複本。
# 兩邊都追蹤 = 兩個主人 —— 翻譯者對 dist 那份送 PR,下一次 cmake --install 會
# 靜默把它蓋回 host/data 的內容,而且不會有任何錯誤訊息。**host/data/ 是唯一
# 來源**,要改就改那裡;package_release.ps1 有一道 sha256 斷言比對兩份,不同就
# 中止打包(代表忘了重跑 cmake --install)。
#
# 剩下的這組與 IsTranslationDataRel()(host/app_update.cpp)畫的是同一條「什麼算
# 翻譯資料」的線,只是換一種語言表達。以前這條線有三份實作(還有打包腳本自己的
# glob 清單);打包腳本那份在 v0.19.0 刪掉了,改成呼叫 `--translation-data-list`
# 走訪 staging、用 IsTranslationDataRel() 本人判定。這裡是唯一剩下的第二份,
# 改一處要同步另一處。
# 一個刻意的差異:atlas_versions/*/atlas_tree_zh.json 不在這裡。輿圖資料是程式
# 抓取重建、再經 prune/fresh 過濾的,手改一份放進 repo 只會在載入時被覆蓋或剝
# 掉,翻譯輿圖不是 PR 形狀的工作。**它同樣不在翻譯資料包裡**(見 app_update.cpp
# 的註解):一個賽季資料夾要三個檔同時存在才會被採納,拆包會讓只裝主檔的人在新
# 賽季開賽當天整季隱形。

# Editor backups, wherever they land. The translation editor writes a .bak
# beside every dictionary it saves, and those sit inside the re-included
# directories above — this line is what keeps them out.
*.bak

# Release packaging output
dist-release/
dist-portable/

# --- Reference / external checkouts (kept local, not uploaded) ---
PathOfBuilding-SimpleGraphic/
atlastree-export/
originalPOB/
contentGGPK/
DebugView/
LibGGPK3-main/
skilltree-export-*/
/Data/

# --- Translation data-generation pipeline (maintainer-only, kept local) ---
/update-translations.bat
/tools/
pob-zh-engine/tools/
/docs/ggpk-translation-pipeline.md

# --- Not part of the public PobTools release (kept local only) ---
/poeMarketTranslate/
# poe-price-check 是獨立產品且有自己的 git repo 與 MIT 授權(大量移植
# Awakened PoE Trade),不隨 PobTools 一起發布。
/poe-price-check/
/poe-price-check-spec.md
# apt-patched(awakened-poe-trade-zh-TW)同理:它是上游 SnosMe 專案的繁中 fork,
# 有自己的 git repo(Hsiung-Shao/awakened-poe-trade-zh-TW)、自己的 MIT 授權與
# 發版流程,只是放在這裡方便一起維護。**絕不可併進 PobTools 的版本控制**,
# 否則會把另一個 repo 的 1.7 GB(含 node_modules 與打包產物)拖進來。
/apt-patched/
# 譯文勘誤協作平台的開發規格。內含 Cloudflare 架構、D1 schema、授權模型與
# 防濫用設計 —— 公開等於把防線畫給要繞過它的人看。規格本身也還沒定案。
/poe-translation-hub-spec.md
# 該規格的實作。理由與上一行相同,而且更強:實作裡有實際的 rate limit 參數、
# 蜜罐欄位名、指紋機制與 RLS 授權模型。另外它有自己的生命週期(上線前會獨立成 repo)
# 且帶 node_modules,不該混進 PobTools 的版本控制。
/poe-translation-hub/
/NeverSinkData/
/zh-rTW/
/filter/
/icon/
/CLAUDE.md
# 發版 SOP skill。與 CLAUDE.md 同性質:維護者流程(發布內部細節、簽章私鑰放在哪、
# -Force 的使用時機),只留本地,不進公開 repo。
/.claude/skills/
/.vscode/
/docs/analysis/
/docs/notes/
/remove/

# --- Secrets(防呆:repo 裡永遠不該出現這些)-----------------
# 發佈簽章的私鑰放在 %USERPROFILE%\.pobtools\ —— 結構上就不在 repo 裡,`git add -A`
# 帶不走。這一段是第二道防線:哪天有人為了「方便」把 .key 複製進工作目錄、或某個
# 腳本把它寫到 repo 底下,git 也不會收。⚠ 兩把私鑰都遺失 = 所有已安裝客戶端永久
# 失去自動更新,但**外流**比遺失更糟 —— 拿到私鑰的人可以簽出一份會被全體客戶端
# 信任的假更新。
#
# 公鑰是另一回事:host/update_pubkeys.h 本來就編進出貨的 exe,公開是它的用途。
*.key
*.pem
*.pfx
*.p12
*.jks
.pobtools/
# 常見的憑證/環境變數外洩形狀
.env
.env.*
!.env.example
secrets.json
*_secret*
*.credentials

# --- Misc --------------------------------------------------
node_modules/
localization/ko-KR/official-terms/.cache/
**/__pycache__/
graphify-out/
image/
images/
Thumbs.db
.DS_Store

# 故障注入用的測試建置與測試安裝(tools/fault_inject.ps1 的 update-repo-gone)。
# ⚠ 那顆 exe 的更新來源被改成一個不存在的 repo,絕對不能進 dist 或出貨包 ——
# package_release.ps1 的 Assert-Step 16 會擋,但擋在那裡等於整個發版流程白做一次。
pob-zh-engine/build-faulttest/
dist-faulttest/

# 維護者翻譯來源資料,只留本地
/CnTranslationData/
/Poe2MarketZh Data/
