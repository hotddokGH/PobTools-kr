# ============================================================
# PobTools — .gitattributes
# ============================================================

# 翻譯字典的行尾是內容的一部分,不是格式偏好。引擎的載入器、翻譯編輯器
# (host/editor_data.cpp)與維護管線(tools/ggpk_zh/apply_changes.py)都刻意
# 逐檔偵測並還原原本的行尾 —— 目前 poe1 / poe2 / launcher 底下的字典是 CRLF,
# item_meta.json、filter_items_zh.json、item_classes_zh.json 是 LF。
#
# 這台機器的 core.autocrlf=true 會在 commit 時把 CRLF 正規化成 LF、checkout 時
# 再轉回 CRLF。對這批檔案那會做兩件壞事:
#
#   1. 那三個 LF 檔在 checkout 後會變成 CRLF,與磁碟上的真實內容不符,而寫回
#      工具會忠實地保留這個被 git 改過的行尾,讓錯誤固定下來。
#   2. 不同平台的貢獻者會為同一份內容算出不同的 blob,於是第一個跨平台 PR 就是
#      整檔數萬行的假 diff —— 真正改動的那一行會被埋掉。
#
# -text 關掉所有換行轉換:存進去什麼位元組,取出來就是什麼位元組。
#
# ⚠ 這一行必須在字典第一次 commit 之前就位。先 commit 再補,CRLF 已經被正規化
# 成 LF 存進歷史,補上之後會再產生一次整檔變動。
pob-zh-engine/dist/Data/**/*.json -text

# 字型是二進位。這台機器的 core.autocrlf=true 會把 .ttf 裡碰巧等於 0x0D 0x0A
# 的位元組當成行尾來轉換,檔案在 checkout 之後就壞了 —— 而且壞得很安靜:
# stb_truetype 解析失敗只會讓那個字型被跳過,fallback 鏈接手,沒有錯誤訊息。
# dep/imgui/.gitattributes 已經替它自己的 ttf 標了 binary,根目錄這份補上
# host/data/fonts/ 這條線。
*.ttf binary

localization/ko-KR/compat/pobtools-ko.patch -text -whitespace
localization/ko-KR/runtime-meta.json -text
pob-zh-engine/dist/Fonts/OFL-NotoSansKR.txt -whitespace
pob-zh-engine/dist/pob-zh.ini -text -whitespace
